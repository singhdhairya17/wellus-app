# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native credentials and release binaries
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.apk
*.aab
*.ipa
*.pem
*.cert

# Sensitive files (avoid committing real secrets under these names)
secrets.json
config.json

# Metro
.metro-health-check*

# Debug logs
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# macOS / editors
.DS_Store
.idea/

# Secrets & local env — copy `.env.example` → `.env.local` after clone (see README)
.env
.env.*
!.env.example
!.env.sample

# Build artifacts
build-*.apk
.build-*.apk

# TypeScript
*.tsbuildinfo

# Native project caches (after `expo prebuild` or local `expo run:*`)
android/app/build/
android/build/
android/.gradle/
.gradle/
ios/Pods/
ios/build/
**/xcuserdata/

# Example app (if folder, add trailing slash)
app-example/
